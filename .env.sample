# AWS Configuration
# Your AWS region (e.g., us-east-1, us-west-2, ap-southeast-1)
AWS_REGION=us-east-1

# Your AWS Account ID (12-digit number)
# CDK can auto-detect this, but you can set it explicitly if needed
AWS_ACCOUNT_ID=123456789012

# CDK Default Configuration
# These are used by CDK CLI for deployment
CDK_DEFAULT_ACCOUNT=123456789012
CDK_DEFAULT_REGION=us-east-1

# Application Environment
# Environment name (e.g., development, staging, production)
ENVIRONMENT=development

# Lambda Configuration
# Log level for Lambda function (e.g., debug, info, warn, error)
LOG_LEVEL=info

# Stack Configuration
# Name prefix for CDK stack (optional, can be used in app.ts)
STACK_NAME=WorkerStack

# Optional: Custom resource naming
# Uncomment and set if you want custom names for resources
# TOPIC_NAME=worker-topic
# QUEUE_NAME=worker-queue
# DLQ_NAME=worker-dlq
